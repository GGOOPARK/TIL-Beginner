#클래스와 객체 개념정리
-클래스: 객체를 정의한 설계도, 틀 (객체를 생성하는 데 사용)
-객체: 클래스에 정의되고 설계된 내용 그대로 생성.

-인스턴스: 클래스를 통해 생성된 객체
-인스턴스화: 클래스로부터 객체를 만드는 과정

-클래스를 정의하기
class 클래스명(대문자로 시작)

-클래스의 4가지요소
(1)필드: 클래스의 속성을 나타내는 함수
(2)메서드: 클래스의 기능을 나타내는 함수
(3)생성자: 클래스의 객체를 생성하는 역할
(4)이너클래스: 클래스 내부에 있는 또다른 클래스
이 중 필드, 메서드, 이너클래스를 클래스의 멤버라고 한다.
필드와 메서드는 각각의 클래스와 관련된 데이터의 집합인 속성과 기능을 대표함
//예시코드
public class HelloWorld {
  int x = 10 // (1)필드
  void printx() {...} // (2)메서드 
  public HelloWorld {...} // (3)생성자
  class HiWorld {...} // (4)이너클래스
  
-객체
:객체는 속성과 기능으로 구성되어 있다. 이 속성과 기능은 필드와 메서드로 정의된다.
-객체의 생성은 new키워드를 사용하여 생성한다. 
//예시코드
class Cafemenu {
  public static void main(String[] arge) {
  
  Cafemenu Americano = new Cafemenu();
  Cafemenu CaffeLatte = new Cafemenu();
즉 클래스명 참조_변수명;
   참조_변수명 = new 생성자(); 으로 객체생성가능.
   클래스명 참조-변수명 = new 생성자(); 으로 간략하게 생성.
   new 키워드는 생성된 객체를 힙메모리에 넣는 명령을 함. 참조변수는 힙메모리의 주소값을 가지고 있다.
    
-객체의 활용
포인트연산자 '.'을 통해 객체의 멤버들에게 접근 가능.
  ex.
  참조 변수명.필드명 // 필드값 불러오기
  참조 변수명.메서드명() // 메서드 호출
  
  
  
